const config = {
  BASE_URL: '', // Leave empty if on localhost
  CLOUDINARY_PATH: '',
  GOOGLE_ANALYTICS_ID: '',
  LOGIN_BOT_USERNAME: '', // Username of the bot that the domain is set in
};

export default config;
